"use strict"; //This use to treat all the JS code as newer version.

// alert(12); This works in browser but we are using nodejs so it won't work as JS engine resides in browser.

/*
number => 2 power 53 range
after this bigInt comes in picture
string = ""
booleasn = true/false
null = it is a value and it is an Object.
undefined = not a value
*/ 