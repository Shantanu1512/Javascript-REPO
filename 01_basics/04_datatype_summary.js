/*

Data types
    Premitive
        String
        Boolean
        Number
        Null
        Undefined

        Symbol
            const id = Symbol('123')
            const cId = Symbol('123')
            console.log(id === cId) even if values are same it will not return true

        BigInt
            const bigNum = 23241n   if we add n then it will be bigInt
    NonPremitive
        Arrays
            const arry = ["Shantanu", "Aishwarya", "Pratik", "Bhumi", "Sandesh"]
        Objects
            const myObj = {
                name: "shantanu"
                age: 24
            }
        Functions
            const myFun = function(){
                
            }

*/